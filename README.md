# disable-ipv6-privacy
Disables IPv6 privacy extensions for NetworkManager profiles to improve IPv6 stability on Linux. Prevents issues caused by temporary IPv6 addresses, such as routing errors, mixed /128 and /64 addresses, and unreliable connectivity in Wi-Fi, VPN, and complex network setups.
